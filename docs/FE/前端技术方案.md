# CoachAI前端技术方案

## 📋 文档信息

| 项目 | 内容 |
|------|------|
| **文档名称** | CoachAI前端技术方案 |
| **文档版本** | V2.0 |
| **创建日期** | 2026-03-29 |
| **最后更新** | 2026-03-29 |
| **负责人** | CoachAI-FE（前端开发专家） |
| **状态** | 正式版 |
| **关联文档** | 产品需求文档(PRD).md, 业务需求文档(BRD).md, 项目产品愿景.md, 项目用户画像.md |

## 🎯 方案概述

### 1.1 核心要求实现
基于用户明确要求，本方案专注于：
1. **前端源码直接服务**：不编译打包，源码直接提供服务
2. **双端分离架构**：用户端（移动H5）和管理端（PC）分离设计
3. **硬件集成范围**：前端仅负责唤起移动端设备的麦克风和摄像头
4. **前后端同仓库**：前端代码和后端代码放在同一仓库中

### 1.2 技术架构特点
- **用户端**：移动H5优先，Vue 3 + Vant 4，硬件唤起功能
- **管理端**：PC优先，Vue 3 + Element Plus，运营管理功能
- **源码服务**：不打包，直接提供ES模块，CDN加载核心库
- **硬件集成**：仅前端唤起，后端处理识别和分析

## 🏗️ 技术架构

### 2.1 双端分离架构
```
用户端（移动H5）             管理端（PC）
    │                          │
    ├── 硬件唤起功能            ├── 租户管理
    ├── 作业批改界面            ├── 用户管理
    ├── 运动识别界面            ├── 系统配置
    ├── 学习管理功能            ├── 数据统计
    └── 家长监督功能            └── 运营工具
            │                          │
            └──────────┬───────────────┘
                       │
               公共基础设施层
            ├── API通信 (Axios)
            ├── 状态管理 (Pinia)
            ├── 路由管理 (Vue Router)
            └── 工具函数库
```

### 2.2 项目结构设计
```
coach-ai-project/
├── src/
│   ├── frontend/                   # 前端源码（直接服务）
│   │   ├── client/                 # 用户端应用（移动H5）
│   │   │   ├── components/         # 组件（作业、运动、学习等）
│   │   │   ├── composables/        # 组合式函数（硬件唤起等）
│   │   │   ├── pages/              # 页面组件
│   │   │   ├── router/             # 路由配置
│   │   │   ├── stores/             # 状态管理
│   │   │   ├── styles/             # 样式文件
│   │   │   ├── types/              # TypeScript类型
│   │   │   ├── utils/              # 工具函数
│   │   │   ├── App.vue             # 根组件
│   │   │   └── main.ts             # 应用入口
│   │   │
│   │   ├── admin/                  # 管理端应用（PC）
│   │   │   ├── components/         # 组件（租户、用户、系统等）
│   │   │   ├── pages/              # 页面组件
│   │   │   ├── router/             # 路由配置
│   │   │   ├── stores/             # 状态管理
│   │   │   ├── styles/             # 样式文件
│   │   │   ├── types/              # TypeScript类型
│   │   │   ├── utils/              # 工具函数
│   │   │   ├── App.vue             # 根组件
│   │   │   └── main.ts             # 应用入口
│   │   │
│   │   ├── shared/                 # 共享代码
│   │   │   ├── components/         # 共享组件
│   │   │   ├── composables/        # 共享组合式函数
│   │   │   ├── stores/             # 共享状态
│   │   │   ├── types/              # 共享类型
│   │   │   └── utils/              # 共享工具
│   │   │
│   │   └── public/                 # 公共资源
│   │       ├── client/             # 用户端HTML入口
│   │       ├── admin/              # 管理端HTML入口
│   │       └── libs/               # 第三方库CDN备用
│   │
│   └── backend/                    # 后端代码目录
│
├── docs/                           # 文档目录
├── scripts/                        # 开发脚本
├── package.json                    # 依赖配置
├── tsconfig.json                   # TypeScript配置
└── README.md                       # 项目说明
```

### 2.3 技术选型

#### 核心框架
| 技术 | 版本 | 适用端 | 加载方式 | 选择理由 |
|------|------|--------|----------|----------|
| **Vue 3** | 3.4+ | 双端 | CDN ES模块 | 现代响应式框架，支持ES模块直接加载 |
| **TypeScript** | 5.0+ | 双端 | 开发时编译 | 类型安全，提高代码质量 |
| **Vue Router** | 4.2+ | 双端 | CDN ES模块 | 官方路由，支持路由守卫 |

#### UI组件库
| 技术 | 版本 | 适用端 | 加载方式 | 选择理由 |
|------|------|--------|----------|----------|
| **Vant 4** | 4.0+ | 用户端 | CDN按需 | 移动端组件库，H5优化 |
| **Element Plus** | 2.3+ | 管理端 | CDN完整 | PC端组件库，管理后台优化 |
| **Tailwind CSS** | 3.3+ | 双端 | CDN CSS | 实用优先，源码友好 |

#### 状态管理和工具
| 技术 | 版本 | 适用端 | 加载方式 | 选择理由 |
|------|------|--------|----------|----------|
| **Pinia** | 2.1+ | 双端 | CDN ES模块 | Vue官方推荐，轻量类型安全 |
| **Axios** | 1.6+ | 双端 | CDN ES模块 | HTTP客户端，支持拦截器 |

#### 硬件集成（仅唤起）
| 技术 | 版本 | 功能 | 加载方式 | 选择理由 |
|------|------|------|----------|----------|
| **WebRTC API** | 浏览器原生 | 摄像头/麦克风唤起 | 浏览器原生 | 标准API，无需额外库 |
| **MediaDevices API** | 浏览器原生 | 设备检测 | 浏览器原生 | 标准API，权限管理 |

## 🔧 源码直接服务实现

### 3.1 HTML入口设计
由于不打包，HTML需要直接加载所有依赖：

**用户端HTML示例 (public/client/index.html)**
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>CoachAI - 智能教育管理</title>
    
    <!-- CSS依赖 -->
    <link rel="stylesheet" href="https://cdn.tailwindcss.com">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/vant@4/lib/index.css">
    <link rel="stylesheet" href="/client/styles/mobile.css">
</head>
<body>
    <div id="app"></div>
    
    <!-- ES模块加载 -->
    <script type="module">
        // 从CDN导入核心库
        import { createApp } from 'https://cdn.jsdelivr.net/npm/vue@3/dist/vue.esm-browser.js';
        import { createRouter } from 'https://cdn.jsdelivr.net/npm/vue-router@4/dist/vue-router.esm-browser.js';
        import { createPinia } from 'https://cdn.jsdelivr.net/npm/pinia@2/dist/pinia.esm-browser.js';
        
        // 按需导入Vant组件
        import { Button, Cell, Uploader } from 'https://cdn.jsdelivr.net/npm/vant@4/lib/vant.es.js';
        
        // 导入本地应用
        import App from '/client/main.js';
        import router from '/client/router/index.js';
        
        // 创建应用
        const app = createApp(App);
        
        // 注册组件
        app.use(Button).use(Cell).use(Uploader);
        app.use(router);
        app.use(createPinia());
        
        // 挂载
        app.mount('#app');
    </script>
</body>
</html>
```

**管理端HTML类似，使用Element Plus组件库**

### 3.2 TypeScript处理
由于源码直接服务，TypeScript需要在开发时编译：

**tsconfig.json关键配置**
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "node",
    "strict": true,
    "outDir": "./dist",
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/frontend/*"],
      "@client/*": ["src/frontend/client/*"],
      "@admin/*": ["src/frontend/admin/*"]
    }
  },
  "include": ["src/frontend/**/*.ts", "src/frontend/**/*.vue"]
}
```

**开发脚本 (scripts/build-frontend.js)**
```javascript
// 监听文件变化并编译TypeScript
const chokidar = require('chokidar');
const { execSync } = require('child_process');

function compileTS() {
  console.log('🔄 编译TypeScript...');
  execSync('npx tsc --project tsconfig.json', { stdio: 'inherit' });
}

// 开发时监听模式
if (process.argv.includes('--watch')) {
  chokidar.watch('src/frontend/**/*.{ts,vue}')
    .on('change', compileTS)
    .on('add', compileTS);
}
```

### 3.3 模块加载优化
1. **CDN加速**：核心库使用CDN，提高加载速度
2. **按需加载**：组件库按需导入，减少初始体积
3. **懒加载**：路由和组件懒加载，提高首屏速度
4. **缓存策略**：Service Worker缓存常用资源

## 📱 移动端硬件唤起

### 4.1 摄像头唤起实现
前端仅负责唤起摄像头，获取视频流：

```typescript
// /client/composables/hardware/useCamera.ts
export function useCamera() {
  const cameraStream = ref<MediaStream | null>(null);
  
  // 唤起摄像头
  async function activateCamera(mode: 'document' | 'motion' = 'document') {
    try {
      const constraints: MediaStreamConstraints = {
        video: {
          facingMode: 'environment',
          ...(mode === 'document' 
            ? { width: { ideal: 1920 }, height: { ideal: 1080 } }
            : { width: { ideal: 1280 }, height: { ideal: 720 }, frameRate: { ideal: 30 } }
          )
        }
      };
      
      const stream = await navigator.mediaDevices.getUserMedia(constraints);
      cameraStream.value = stream;
      return stream;
    } catch (error) {
      throw handleCameraError(error);
    }
  }
  
  // 拍照（获取当前帧）
  function takePhoto(videoElement: HTMLVideoElement): string {
    const canvas = document.createElement('canvas');
    canvas.width = videoElement.videoWidth;
    canvas.height = videoElement.videoHeight;
    
    const ctx = canvas.getContext('2d')!;
    ctx.drawImage(videoElement, 0, 0);
    
    // 转换为base64发送给后端
    return canvas.toDataURL('image/jpeg', 0.8);
  }
  
  // 停止摄像头
  function deactivateCamera() {
    cameraStream.value?.getTracks().forEach(track => track.stop());
    cameraStream.value = null;
  }
  
  return { cameraStream, activateCamera, takePhoto, deactivateCamera };
}
```

### 4.2 麦克风唤起实现
```typescript
// /client/composables/hardware/useMicrophone.ts
export function useMicrophone() {
  const microphoneStream = ref<MediaStream | null>(null);
  
  async function activateMicrophone() {
    try {
      const stream = await navigator.mediaDevices.getUserMedia({
        audio: {
          echoCancellation: true,
          noiseSuppression: true,
          sampleRate: 48000
        }
      });
      microphoneStream.value = stream;
      return stream;
    } catch (error) {
      throw handleMicrophoneError(error);
    }
  }
  
  // 录制音频
  function recordAudio(duration: number = 5000): Promise<Blob> {
    return new Promise((resolve, reject) => {
      if (!microphoneStream.value) {
        reject(new Error('麦克风未激活'));
        return;
      }
      
      const mediaRecorder = new MediaRecorder(microphoneStream.value);
      const audioChunks: Blob[] = [];
      
      mediaRecorder.ondataavailable = (event) => {
        audioChunks.push(event.data);
      };
      
      mediaRecorder.onstop = () => {
        resolve(new Blob(audioChunks, { type: 'audio/webm' }));
      };
      
      mediaRecorder.start();
      setTimeout(() => mediaRecorder.stop(), duration);
    });
  }
  
  return { microphoneStream, activateMicrophone, recordAudio };
}
```

### 4.3 设备兼容性处理
```typescript
// /client/utils/device-detector.ts
export class DeviceDetector {
  // 检测摄像头支持
  static async checkCameraSupport() {
    try {
      const devices = await navigator.mediaDevices.enumerateDevices();
      const videoDevices = devices.filter(d => d.kind === 'videoinput');
      return { supported: videoDevices.length > 0, devices: videoDevices };
    } catch {
      return { supported: false, devices: [] };
    }
  }
  
  // 检测浏览器信息
  static getDeviceInfo() {
    const ua = navigator.userAgent;
    return {
      isMobile: /iPhone|iPad|iPod|Android/i.test(ua),
      isIOS: /iPhone|iPad|iPod/i.test(ua),
      isAndroid: /Android/i.test(ua),
      isWeChat: /MicroMessenger/i.test(ua)
    };
  }
}
```

### 4.4 权限引导和错误处理
```vue
<!-- /client/components/common/PermissionGuide.vue -->
<template>
  <div v-if="!permissionGranted" class="permission-guide">
    <h3>需要摄像头/麦克风权限</h3>
    <p>请按照以下步骤开启权限：</p>
    
    <div class="steps">
      <div class="step">1. 点击浏览器弹出的"允许"按钮</div>
      <div class="step">2. 或在浏览器设置中手动开启权限</div>
      <div class="step">3. 刷新页面重新尝试</div>
    </div>
    
    <button @click="retryPermission" class="btn btn-primary">
      重新尝试获取权限
    </button>
  </div>
</template>
```

## 🖥️ 管理端PC功能

### 5.1 管理端技术特点
1. **PC优先设计**：1440px基准，鼠标交互优化
2. **Element Plus组件库**：适合管理后台的组件
3. **复杂数据展示**：表格、图表、表单等
4. **批量操作支持**：多选、批量处理等

### 5.2 核心管理功能
1. **租户管理**
   - 家庭租户列表和详情
   - 租户状态管理（启用/禁用）
   - 租户数据统计

2. **用户管理**
   - 家庭成员管理
   - 用户权限配置
   - 使用情况监控

3. **系统配置**
   - 全局参数配置
   - 功能开关管理
   - 系统日志查看

4. **数据统计**
   - 使用数据报表
   - 业务数据图表
   - 导出功能

### 5.3 管理端组件示例
```vue
<!-- /admin/components/tenant/TenantTable.vue -->
<template>
  <el-table :data="tenants" style="width: 100%">
    <el-table-column prop="name" label="家庭名称" width="180" />
    <el-table-column prop="memberCount" label="成员数" width="100" />
    <el-table-column prop="createdAt" label="创建时间" width="180" />
    <el-table-column prop="status" label="状态" width="100">
      <template #default="scope">
        <el-tag :type="scope.row.status === 'active' ? 'success' : 'info'">
          {{ scope.row.status === 'active' ? '活跃' : '已禁用' }}
        </el-tag>
      </template>
    </el-table-column>
    <el-table-column label="操作" width="200">
      <template #default="scope">
        <el-button size="small" @click="viewTenant(scope.row)">
          查看
        </el-button>
        <el-button size="small" type="primary" @click="editTenant(scope.row)">
          编辑
        </el-button>
        <el-button 
          size="small" 
          :type="scope.row.status === 'active' ? 'danger' : 'success'"
          @click="toggleTenantStatus(scope.row)"
        >
          {{ scope.row.status === 'active' ? '禁用' : '启用' }}
        </el-button>
      </template>
    </el-table-column>
  </el-table>
</template>

<script setup lang="ts">
import { ref } from 'vue';
import { getTenants, updateTenantStatus } from '@/api/admin/tenants';

const tenants = ref<any[]>([]);

// 加载租户数据
async function loadTenants() {
  tenants.value = await getTenants();
}

// 切换租户状态
async function toggleTenantStatus(tenant: any) {
  const newStatus = tenant.status === 'active' ? 'inactive' : 'active';
  await updateTenantStatus(tenant.id, newStatus);
  loadTenants(); // 重新加载
}

loadTenants();
</script>
```

## 🔄 前后端协作

### 6.1 数据流设计
```
前端职责：
1. 用户界面展示
2. 用户交互处理
3. 硬件设备唤起
4. 数据格式转换（图片→base64，音频→Blob）

后端职责：
1. 业务逻辑处理
2. AI识别和分析（OCR、动作识别、语音识别）
3. 数据存储和管理
4. 多租户数据隔离

数据流：
前端采集 → 格式转换 → API发送 → 后端处理 → 返回结果 → 前端展示
```

### 6.2 API接口设计
```typescript
// 作业批改相关API
interface HomeworkAPI {
  // 提交作业图片（前端拍照→base64→后端OCR）
  submitHomeworkImage(imageData: string): Promise<{
    taskId: string;
    result: HomeworkResult;
  }>;
  
  // 获取批改历史
  getHomeworkHistory(studentId: string): Promise<HomeworkRecord[]>;
}

// 运动识别相关API
interface SportsAPI {
  // 提交运动视频（前端录制→Blob→后端动作识别）
  submitSportsVideo(videoBlob: Blob): Promise<SportsResult>;
  
  // 实时运动识别（WebSocket）
  startRealTimeSports(): WebSocket;
}

// 管理端API
interface AdminAPI {
  // 租户管理
  getTenants(): Promise<Tenant[]>;
  updateTenantStatus(tenantId: string, status: string): Promise<void>;
  
  // 用户管理
  getUsers(tenantId?: string): Promise<User[]>;
  
  // 数据统计
  getStatistics(dateRange: DateRange): Promise<Statistics>;
}
```

### 6.3 错误处理和状态管理
```typescript
// 统一的API错误处理
import axios from 'axios';

const api = axios.create({
  baseURL: '/api',
  timeout: 10000
});

// 请求拦截器
api.interceptors.request.use(config => {
  // 添加认证token
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  
  // 添加租户ID
  const tenantId = localStorage.getItem('tenant_id');
  if (tenantId) {
    config.headers['X-Tenant-ID'] = tenantId;
  }
  
  return config;
});

// 响应拦截器
api.interceptors.response.use(
  response => response.data,
  error => {
    // 统一错误处理
    if (error.response?.status === 401) {
      // 未授权，跳转到登录页
      window.location.href = '/login';
    } else if (error.response?.status === 403) {
      // 权限不足
      showErrorMessage('权限不足，请联系管理员');
    } else if (error.response?.status === 429) {
      // 请求过于频繁
      showErrorMessage('操作过于频繁，请稍后重试');
    } else {
      // 其他错误
      showErrorMessage(error.response?.data?.message || '网络错误');
    }
    
    return Promise.reject(error);
  }
);
```

## 🎯 技术方案总结

### 7.1 方案优势
1. **源码直接服务**：无需构建打包，开发部署简单
2. **双端分离设计**：用户端和管理端针对不同场景优化
3. **硬件集成清晰**：前端仅负责唤起，职责明确
4. **技术栈统一**：Vue 3统一技术栈，降低学习成本
5. **CDN加速**：核心库使用CDN，提高加载速度

### 7.2 关键技术点
1. **ES模块加载**：使用`<script type="module">`直接加载Vue等库
2. **TypeScript开发**：开发时编译，运行时使用JavaScript
3. **按需加载**：组件库按需导入，减少初始体积
4. **硬件API标准化**：使用WebRTC标准API，兼容性好

### 7.3 实施建议
1. **分阶段实施**：先实现用户端核心功能，再开发管理端
2. **渐进式增强**：基础功能先行，高级功能后续添加
3. **兼容性测试**：重点测试移动端浏览器兼容性
4. **性能监控**：监控页面加载时间和硬件使用情况

### 7.4 风险控制
1. **浏览器兼容性**：使用标准API，提供降级方案
2. **网络稳定性**：CDN备用方案，本地资源备份
3. **硬件可用性**：设备检测，权限引导，错误处理
4. **代码维护**：严格代码规范，完善文档

## 📋 附录

### A. 浏览器兼容性要求
- **移动端**：iOS Safari 14+, Android Chrome 90+, 微信浏览器
- **PC端**：Chrome 90+, Firefox 88+, Edge 90+
- **硬件API**：支持WebRTC和MediaDevices API

### B. 开发环境要求
- Node.js 18+
- TypeScript 5.0+
- 代码编辑器（VS Code推荐）
- 移动端测试设备

### C. 相关资源
1. **Vue 3文档**：https://vuejs.org/
2. **Vant 4文档**：https://vant-ui.github.io/vant/
3. **Element Plus文档**：https://element-plus.org/
4. **WebRTC文档**：https://webrtc.org/
5. **Tailwind CSS文档**：https://tailwindcss.com/

---

**文档版本**: V2.0  
**创建时间**: 2026-03-29  
**更新时间**: 2026-03-29  
**负责人**: CoachAI-FE（前端开发专家）  
**状态**: 正式版

**文档说明**:
本技术方案基于用户明确要求制定，专注于：
1. 前端源码直接服务的技术实现
2. 用户端（移动H5）和管理端（PC）的架构设计
3. 移动端硬件唤起的具体实现
4. 前后端职责划分和协作方式

方案已准备就绪，可立即开始实施。